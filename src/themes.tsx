export type Theme = {
  primaryColor: string;
  secondaryColor: string;
  highlightColor: string;
  opositeColor: string;
}

export const defaultTheme: Theme = {
  primaryColor: "red",
  secondaryColor: "red",
  highlightColor: "red",
  opositeColor: "red",
}